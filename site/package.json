{
  "name": "muamba-site",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "generate": "go run ./cmd/generate",
    "dev": "npm run generate && npm run build && wrangler dev",
    "build": "node scripts/build-static.mjs",
    "deploy": "npm run generate && npm run build && wrangler deploy",
    "test": "npm run generate && npm run build && node --test tests/*.test.mjs",
    "lint": "node --check scripts/build-static.mjs"
  },
  "type": "module"
}
